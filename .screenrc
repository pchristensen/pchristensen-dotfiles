hardstatus alwayslastline
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{=kw}%?%-Lw%?%{r}(%{W}%n*f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B}%Y-%m-%d %{W}%c %{g}]'

escape ^Zz

vbell off 

autodetatch on

defscrollback 30000

# Default Screens
screen -t shell 0
screen -t shell 1
screen -t emacs 2 emacsclient -t

